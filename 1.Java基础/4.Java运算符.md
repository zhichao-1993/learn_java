* [Java运算符](#java运算符)
  * [1. 算术运算符](#1-算术运算符)
     * [1.1 概述](#11-概述)
  * [2. 关系运算符](#2-关系运算符)
     * [2.1 概述](#21-概述)
  * [3. 位运算符(不常用)](#3-位运算符不常用)
  * [4. 逻辑运算符](#4-逻辑运算符)
     * [4.1 概述](#41-概述)
     * [4.2 短路运算(&amp;&amp;和||)](#42-短路运算和)
  * [5. 赋值运算符](#5-赋值运算符)
     * [5.1 概述](#51-概述)
  * [6. 其他运算符](#6-其他运算符)
     * [6.1 条件运算符（? :）](#61-条件运算符-)
     * [6.2 instanceof 运算符](#62-instanceof-运算符)
  * [7. 运算符的优先级](#7-运算符的优先级)
# Java运算符
## 1. 算术运算符 
### 1.1 概述
  + 执行加减乘除四则运算的运算符

在例子中，初始化两个整型变量a、b：int a = 2; int b = 4;    

|运算符	|描述	|例子|
|:---:|:---:|:---:|
|+|	加法运算符 （也用于字符串连接）|	a + b 等于 6
|-	|减法运算符|	a - b 等于 -2
|*	|乘法运算符|	a * b 等于 8
|/	|除法运算符|	b / a 等于 2
|%	|取余运算符	|b % a 等于 0
|++	|自增运算符	|a ++ 等于 3
|--	|自减运算符	|b – 等于 3

>注意：
>
>1. 每一个变量的前面，都要声明它的**类型**
>2. **不同类型**的数据是不可以进行运算的
>3. 2/4运算，在整数里面为0，在浮点数里为0.5。所以想要得到小数，就是用浮点数。浮点数后面必须带上F或f。


---
## 2. 关系运算符
### 2.1 概述
  + 比较运算符
  
|运算符	|描述	|
|:---:|:---:|
|==	|检查如果两个操作数的值是否相等，如果相等则条件为真。	(a == b) 
|!=	|检查如果两个操作数的值是否相等，如果值不相等则条件为真。	(a != b) 
|>	|检查左操作数的值是否大于右操作数的值，如果是那么条件为真。	(a > b) 
|<	|检查左操作数的值是否小于右操作数的值，如果是那么条件为真。	(a < b)
|>=|	检查左操作数的值是否大于或等于右操作数的值，如果是那么条件为真。	(a >= b)
|<=	|检查左操作数的值是否小于或等于右操作数的值，如果是那么条件为真。	(a <= b)

> 注意：
>
> 1.比较两个操作数的时候，必须"=="而不是"="

---
## 3. 位运算符(不常用)

  + 概述：对整数类型进行按位和移位操作的运算符
  + 举例：假设a = 60，b = 13;它们的二进制格式表示将如下：


    a = 0011 1100
    b = 0000 1101
    -----------------
    a & b = 0000 1100
    a | b = 0011 1101
    a ^ b = 0011 0001
    ~a = 1100 0011


  
下表列出了位运算符的基本运算，假设整数变量 a 的值为 60 和变量 b 的值为 13：

|运算符|	描述|	例子|
|:---:|:---:|:---:|
|＆（按位与）|	如果相对应位都是1，则结果为1，否则为0	|（a＆b），得到12，即0000 1100
||（按位或）|	如果相对应位都是 0，则结果为 0，否则为 1	|（a | b）得到61，即 0011 1101
|^（按位异或）	|如果相对应位值相同，则结果为0，否则为1	|（a ^ b）得到49，即 0011 0001
|〜（按位取反）	|按位取反运算符翻转操作数的每一位，即0变成1，1变成0。	|（〜a）得到-61，即1100 0011
|<< （左位移）	|按位左移运算符。左操作数按位左移右操作数指定的位数。|	a << 2得到240，即 1111 0000
|>>（右位移）	|按位右移运算符。左操作数按位右移右操作数指定的位数。|	a >> 2得到15即 1111
| >>> （零填充右移）	|按位右移补零操作符。左操作数的值按右操作数指定的位数右移，移动得到的空位以零填充。	|a>>>2得到15即0000 1111

---
## 4. 逻辑运算符
### 4.1 概述
  + 逻辑运算符可以在表达式中生成组合条件
  + 举例：初始化两个整型变量a、b：int a = 0; int b = 1;

|运算符	|描述|	例子|
|:---:|:---:|:---:|
|&&（逻辑与）	|当且仅当两个操作数都为真，条件才为真。	|(a && b)为假
|两条竖线（逻辑或）	|如果任何两个操作数任何一个为真，条件为真。	|(a 两条竖线 b)为真
|！（逻辑非）	|用来反转操作数的逻辑状态。如果条件为真，则逻辑非运算符将得到假。	|!(a && b)为假

### 4.2 短路运算(&&和||)
  + 当第一个条件能够得到运算结果的时候，那第二个条件就不会去执行了，这个就是短路
  

        class LogicOperators {
            public static void main(String[] args){
                int a = 1, b = 2;
                if((a == 2) && (b == 2)) {
                    System.out.println("a和b都等于2");
                }
                if((a == 1) || (b == 1)) {
                    System.out.println("a等于1或b等于1");
                }
            }
        }
        //运行结果：a等于1或b等于1



---
## 5. 赋值运算符
### 5.1 概述
 + 为指定的变量分配值

|运算符	|描述|	例子|
|:---:|:---:|:---:|
|=	|简单的赋值运算符。将值从右侧操作数分配给左侧操作数。|	c = a + b将a + b的值赋给c
|+=	|加和赋值运算符。它将右操作数添加到左操作数，并将结果分配给左操作数。|	c + = a等于c = c + a
|-=	|减和赋值运算符。它从左侧操作数中减去右侧操作数，并将结果分配给左侧操作数。|	c -= a等效于c = c – a
|*=|	乘和赋值运算符。它将右操作数与左操作数相乘，并将结果分配给左操作数。|	c *= a等效于c = c * a
|/ =	|除和赋值运算符。它将左操作数除以右操作数，并将结果分配给左操作数。 |c /= a等于c = c / a

---
## 6. 其他运算符
### 6.1 条件运算符（? :）
  + 变量 = (布尔表达式) ? 值1 : 值2
  + 目的：确定将那个值分配给变量
  + 如果为True，把值1赋值给变量；False，把值2赋值给变量

        public class ConditionalOperators {
            public static void main(String[] args) {
                int age = 15;
                System.out.println(age >= 18 ? "在中国你已经成年" :  "在中国你还未成年"); 
            }
        }
        //运行结果：在中国你还未成年
        
### 6.2 instanceof 运算符
 + 左侧变量所指的对象，是右侧类或接口的对象，结果为true，否则为false
 +  boolean b = name instanceof String;
 
        public class InstanceOfOperators1 {
            public static void main(String[] args) {
                String name = "imooc";
                boolean b = name instanceof String; 
                  System.out.println("结果为：" + b);
            }
        }
> 注意：
>
> 1.instanceof运算符不能操作**8大基本计算类型**

---

## 7. 运算符的优先级
|类别	|操作符|	关联性|
|:---:|:---:|:---:|
|后缀	|() [] . (点操作符)	|左到右
|一元	|+ + - ！〜	|从右到左
|乘性	|* /％	|左到右
|加性	|+ -|	左到右
|移位|	>> >>> <<	|左到右
|关系	|>> = << =|	左到右
|相等|	== !=	|左到右
|按位与	|＆|	左到右
|按位异或|	^	|左到右
|按位或	|	竖|左到右
|逻辑与|	&&	|左到右
|逻辑或|	两条竖线	|左到右
|条件|	？：	|从右到左
|赋值|	= + = - = * = / =％= >> = << =＆= ^ = | =|	从右到左
|逗号|	，	|左到右

> 注意：
>
> 1.一元，条件，赋值 优先级**从右到左**